doctype html
head
  title Copisteria Julian
  meta(charset='utf-8')
  meta(name='viewport' content='width=device-width, initial-scale=1')
  link(rel='stylesheet' href='http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css')
  script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js')
  script(src='http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js')
  script(src="/javascripts/script.js" type="module", defer)
<!--  script(src='https://cdnjs.cloudflare.com/ajax/libs/pug/3.0.2/pug.min.js', defer) -->
  style.
    .tinta {
      width: 50px;
      height: 50px;
      float: left;
      padding: 5px;
    } 

    .impresoras-container{
      display: flex;
      justify-content: space-around;
    }

    .toner {
      display: flex;
    }

    img{
      cursor:pointer;
    }

.container
  .row
    h3 Hojas:
     .contadorHojas
    p Introduce la impresora y el texto a imprimir
    form.form-inline(action='index.php' method='get' role='form')
      | Impresora: 
      select.form-control(name='n_impresora' id='n_impresora')
        option(value='A2') A2
        option(value='A3') A3
        option(value='A4') A4
  textarea.form-control(rows='4' name='texto' id='texto')
  input(type='submit' value='Enviar a impresora' id='enviar')
.container
  .row
  .impresoras-container